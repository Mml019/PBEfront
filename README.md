# PBEfront
PBE frontend make it with react and vite. Also used librarys as react hook form, react bootstrap and yup.

To running this proyect i installed vite and used npm to manage node:

Run files with vite:


And then use npm install command. This command generate you a build file.
npm install

npm run build or in development npm run dev.